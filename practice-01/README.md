# Практическая работа №1. Основы языка JavaScript и работа с Git

* **Студент:** Захаркина Наталья Дмитриевна
* **Группа:** ЭФБО-04-25
* **Вариант:** 6



## 1. Сведения об окружении
* **Операционная система:** macOS
* **Версия Node.js:** 24.21.0
* **Версия npm:** 11.19.0
* **Версия Git:** 2.33.0



## 2. Результаты выполнения заданий

### Задание 1. Проверка сред выполнения
* **Вывод в Node.js (терминал):** `Тип document: undefined`
* **Вывод в браузере (консоль DevTools):** `Тип document: object`
* **Вывод:** Среда Node.js является серверной и не имеет встроенных объектов для работы с веб-страницами (таких как `document`), поэтому его тип `undefined`. Браузерная среда предназначена для работы с HTML-документами, поэтому там этот объект существует и имеет тип `object`.

### Задание 2. Исследование типов и преобразований

| № | Выражение | Значение | Тип | Объяснение |
|---|---|---|---|---|
| 1 | `"8" + 2` | `"82"` | `string` | Конкатенация. Оператор `+` при встрече со строкой склеивает данные в текст. |
| 2 | `"8" - 2` | 6 | `number` | Оператор `-` не работает со строками и автоматически приводит `"8"` к числу. |
| 3 | `Number("8") + 2` | 10 | `number` | Явное преобразование строки в число 8 с последующим сложением. |
| 4 | `"12" > "3"` | `false` | `boolean` | Посимвольное сравнение строк. Первый символ `"1"` меньше, чем `"3"`. |
| 5 | `12 === "12"` | `false` | `boolean` | Строгое равенство проверяет и значение, и тип. Число не равно строке. |
| 6 | `Number("")` | 0 | `number` | Пустая строка при явном преобразовании в число всегда возвращает 0. |
| 7 | `Number("text")` | `NaN` | `number` | Строку с текстом невозможно преобразовать в число, возвращается `NaN`. |
| 8 | `Boolean("false")` | `true` | `boolean` | Любая непустая строка в JS при логическом приведении даёт `true`. |
| 9 | `typeof null` | `"object"` | `string` | Исторический баг дизайна JS. `typeof` возвращает текстовый результат `"object"`. |
| 10 | `typeof NaN` | `"number"` | `string` | `NaN` технически относится к типу `number`. `typeof` возвращает строку `"number"`. |

### Задание 3. Расчет прогресса задач

| Проверка | Входные данные | Ожидаемый результат | Фактический результат | Итог |
|---|---|---|---|---|
| Обычный случай (Вариант 6) | `total=18`, `completed=6` | Осталось: 12; Прогресс: 33.3%; Статус: В работе | Осталось: 12; Прогресс: 33.3%; Статус: В работе | Пройдено |
| Нулевой баланс задач | `total=0`, `completed=0` | Сообщение «Задач пока нет» | Сообщение «Задач пока нет» | Пройдено |
| Старт выполнения работы | `total=5`, `completed=0` | Осталось: 5; Прогресс: 0.0%; Статус: Не начато | Осталось: 5; Прогресс: 0.0%; Статус: Не начато | Пройдено |
| Полное завершение работы | `total=5`, `completed=5` | Осталось: 0; Прогресс: 100.0%; Статус: Завершено | Осталось: 0; Прогресс: 100.0%; Статус: Завершено | Пройдено |
| Ошибка: избыток выполнения | `total=5`, `completed=6` | Ошибка: выполнено больше, чем существует | Ошибка: выполнено больше, чем существует | Пройдено |
| Ошибка: отрицательный ввод | `total=-1`, `completed=0` | Ошибка: отрицательное количество | Ошибка: отрицательное количество | Пройдено |
| Ошибка: дробное количество | `total=5`, `completed=2.5` | Ошибка: дробное количество | Ошибка: дробное количество | Пройдено |
| Ошибка: неверный тип данных | `total="5"`, `completed=2` | Ошибка: вместо числа передана строка | Ошибка: вместо числа передана строка | Пройдено |
| Ошибка: выход за верхний лимит| `total=1001`, `completed=0` | Ошибка: превышена верхняя граница | Ошибка: превышена upper bound | Пройдено |
| Ошибка: некорректное число `NaN`| `total=NaN`, `completed=0` | Ошибка: недопустимое числовое значение | Ошибка: недопустимое числовое значение | Пройдено |

### Задание 4. Пошаговое планирование графиков

| Проверка | Входные данные | Ожидаемый результат | Фактический результат | Итог |
|---|---|---|---|---|
| Обычный случай (Вариант 6) | `18`, `6`, `5` | 3 дня (День 1: 5, День 2: 5, День 3: 2) | Потребуется дней: 3 | Пройдено |
| Общий пример из ТЗ | `12`, `5`, `3` | 3 дня (в последний рабочий день 1 задача) | Потребуется дней: 3 | Пройдено |
| Равномерное распределение | `10`, `4`, `3` | 2 дня (по 3 задачи ежедневно) | Потребуется дней: 2 | Пройдено |
| Избыточный дневной лимит | `5`, `3`, `10` | 1 день (выполнено 2 оставшиеся задачи) | Потребуется дней: 1 | Пройдено |
| Уже выполненный проект | `5`, `5`, `2` | 0 дней (цикл планирования не запускается) | Потребуется дней: 0 | Пройдено |
| Проект без задач на старте | `0`, `0`, `2` | 0 дней (цикл планирования не запускается) | Потребуется дней: 0 | Пройдено |
| Ошибка: нулевой лимит дня | `5`, `2`, `0` | Ошибка; цикл не запускается | Ошибка; цикл не запускается | Пройдено |
| Ошибка: нецелый лимит дня | `5`, `2`, `1.5` | Ошибка: дробной дневной нормы быть не должно | Ошибка: дробной нормы быть не должно | Пройдено |
| Ошибка: избыток выполнения | `5`, `6`, `2` | Ошибка: некорректное число выполненных задач | Ошибка: некорректное число задач | Пройдено |
| Ошибка: лимит задан строкой | `5`, `2`, `"2"` | Ошибка: дневная норма задана строкой | Ошибка: дневная норма задана строкой | Пройдено |
| Ошибка: превышение границ нормы| `5`, `2`, `1001` | Ошибка: превышена верхняя граница нормы | Ошибка: превышена верхняя граница нормы | Пройдено |

### Задание 5. Исправление логических ошибок (Отладка)
В ходе выполнения задания были обнаружены и исправлены две ошибки:
1. **Обработка количества задач:** Строковые переменные склеивались оператором `+` (`"3" + "2" = "32"`). Было добавлено явное приведение типов с помощью `Number()`.
2. **Граница цикла:** Из-за строгого условия `< 4` цикл не учитывал четвертую задачу. Было исправлено на нестрогое неравенство `<= 4`.

#### Иллюстрация остановленной программы (Точка останова)
![Отладка](./screenshots/debug-breakpoint.png)

#### Фактический вывод после исправления:
```text
Выполнено: 5
Осталось: 3
Контрольная сумма: 10
```



## 3. Дополнительное задание (Вариант А)
Было реализовано расширение, обрабатывающее входные данные в виде строк (файл `progress-input.js`). Перед вычислениями строки очищаются методом `.trim()`.

* **Ответ на контрольный вопрос:** Почему недостаточно просто вызвать `Number()` и проверить, что результат неотрицателен?
* **Ответ:** Функция `Number()` преобразует пустые строки `""`, строки из пробелов `"   "`, а также значение `null` в число `0`. Из-за этого программа приняла бы некорректный пустой ввод за правильное нулевое число задач. Для предотвращения этого необходимы предварительные строкие проверки типов и пустых строк.